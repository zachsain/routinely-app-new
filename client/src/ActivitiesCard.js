import React, {useState} from 'react'

function ActivitiesCard({
    title,
    category, 
    duration, 
    description,
    likes,
    routine_id
}){

    return(
        <div>{title}</div>
    )
}